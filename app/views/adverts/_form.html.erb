<%= simple_form_for(advert) do |f| %>
  <%= f.input :name %>
  <%= f.input :type_ad, collection: ['Lost', 'Find', 'See'] %>
  <%= f.input :phone %>
  
  <%= f.input :address %>
  <%= f.input :content %>
  <%= f.submit %>
<% end %>